{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "prisma generate --schema=prisma/schema.prisma && pnpm build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
